<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.hp.bhelstatus">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainField"></activity>
        <activity android:name=".DeptList"></activity>
        <activity android:name=".ProjectList"
            android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".ProjectAdd"></activity>
        <activity android:name=".ProjectView"
            android:parentActivityName=".ProjectList"></activity>
        <activity android:name=".FieldChange"></activity>
    </application>

</manifest>